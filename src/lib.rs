pub mod api;
pub mod firecrawl_client;
pub mod models;
pub mod parser;
pub mod pick_n_pull;

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
